version: '3'
services:
  nginx:
    build:
      context: ./requirements/nginx/
      dockerfile: Dockerfile
    ports:
      - "443:443"

  wordpress:
    build:
      context: ./requirements/wordpress/
      dockerfile: Dockerfile
    ports:
      - "9000:9000"
